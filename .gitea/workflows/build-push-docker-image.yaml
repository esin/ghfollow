name: Build docker image

on:
  push:
    branches:
      - ${CI_REPO_DEFAULT_BRANCH}

  workflow_dispatch:

jobs:
  docker-build:
    runs-on: docker-build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.HUB_DOCKER_USERNAME }}
          password: ${{ secrets.HUB_DOCKER_PASSWORD }}

      - name: Generate date-based tag
        id: new-tag
        run: |
          NEWTAG=$(date +'date +%y%m%d%H%M')
          echo "Generated tag: $NEWTAG"
          echo "::set-output name=tag::$NEWTAG"

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          tags: ${{ steps.new-tag.outputs.tag }}
          push: true

      # - name: Read tags from file
      #   id: tags
      #   run: |
      #     TAGS=$(cat .tags)
      #     echo "::set-output name=tags::$TAGS"